local SimpleMath = {}
function SimpleMath.add(x, y)
  return x + y
end

function SimpleMath.subtract(x, y)
  return x - y
end

function SimpleMath.multiply(x,y)
  return x * y
end

function SimpleMath.divide(x, y)
  return x / y
end
return SimpleMath
